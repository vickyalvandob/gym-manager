---
paths:
  - 'resources/js/pages/payments/**'
---

# Payments

## Separate payment types with tabs
The payment workspace has exactly two primary tabs: Membership and Personal Trainer. Keep the type selector out of the filter form, default to Membership, retain only operational filters, and use a compact table on wide screens with cards below xl.
